Collection of dotfiles.
1. Need to manually do ln -s dotfiles/file ~/.file
2. For ViM remember to install Vundle 
   [git clone https://github.com/gmarik/vundle.git ~/.vim/bundle/vundle]
   In Vim run :BundleInstall
